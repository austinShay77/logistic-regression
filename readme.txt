Libraries needed:

    math
    matplotlib

To run the program:

    The listed libraries need to be installed, and the .data file needs to be present at the same level as
    the hw3.py file.

    To run the program: python3 ./hw3.py

    On run it should print the data requested and show the plot for the log loss, epoch relationship.